#include <stdio.h>
#define PI 3.14

void main()
{
   double area = 0.0, radius = 0.0;
   printf("Enter Radius:");
   scanf("%lf", &radius);
   area= PI * radius * radius;
   printf("radius of %lf meters: area is %f sq. meters \n", radius, area);
    
}
